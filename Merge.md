#ќператоры, допускающие логическое значение NULL
*ƒл€ операндов bool? операторы & (логическое ») и | (логическое »Ћ») поддерживают следующую логику с трем€ значени€ми:

*ќператор & возвращает true только в том случае, если оба операнда имеют значение true. ≈сли x или y имеет значение false, оператор x & y возвращает false (даже если другой операнд имеет значение null). ¬ противном случае выражение x & y будет иметь значение null.

*ќператор | возвращает false только в том случае, если оба операнда имеют значение false. ≈сли x или y имеет значение true, оператор x | y возвращает true (даже если другой операнд имеет значение null). ¬ противном случае выражение x | y будет иметь значение null.